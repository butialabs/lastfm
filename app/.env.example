# Application URL (used in OAuth redirects)
APP_URL=http://localhost:8080

# Database
DB_CONNECTION=sqlite
SQLITE_PATH=data/db/lastfm.sqlite

# Last.fm API
LASTFM_API=your_lastfm_api_key

# Last.fm API retry settings
LASTFM_MAX_RETRIES=3

# MusicBrainz as primary image source (optional)
# If true, searches MusicBrainz first and uses Last.fm as a fallback
# If false or undefined, uses Last.fm as the primary source and MusicBrainz as a fallback
MUSICBRAINZ_DEFAULT=false

# Security (32 characters recommended)
ENCRYPTION_KEY=your_32_char_encryption_key_123456

# Admin access
ADMIN_USER=admin
ADMIN_PASSWORD=change_this_password